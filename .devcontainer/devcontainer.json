{
    "name": "study-llm",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },

    "runArgs": [
        // GPU support
        "--gpus=all",
        "--privileged=true"
    ]
}